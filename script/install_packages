#!/bin/bash -x

# sudo apt-add-repository "deb http://archive.canonical.com/ubuntu $(lsb_release -sc) partner"
# sudo apt-add-repository 'ppa:ubuntu-elisp/ppa'
# sudo apt-add-repository 'ppa:nesthib/weechat-stable'
# sudo apt-add-repository "deb https://apt.dockerproject.org/repo ubuntu-$(lsb_release -sc) main"
# sudo apt-key adv \
#    --keyserver 'pgp.mit.edu' \
#    --recv-keys '58118E89F3A912897C070ADBF76221572C52609D'
# sudo apt-add-repository 'ppa:zeal-developers/ppa'
# sudo apt-key adv --keyserver 'pgp.mit.edu' --recv-keys '5044912E'
# sudo apt-add-repository "deb http://linux.dropbox.com/ubuntu/ $(lsb_release -sc) main"

# sudo apt-get update

# sudo cat >/etc/yum.repos.d/docker.repo <<-EOF
# [dockerrepo]
# name=Docker Repository
# baseurl=https://yum.dockerproject.org/repo/main/fedora/22
# enabled=1
# gpgcheck=1
# gpgkey=https://yum.dockerproject.org/gpg
# EOF

rpm --quiet --query folkswithhats-release || sudo dnf -y --nogpgcheck install http://folkswithhats.org/repo/$(rpm -E %fedora)/RPMS/noarch/folkswithhats-release-1.0.0-1.fc$(rpm -E %fedora).noarch.rpm
rpm --quiet --query rpmfusion-free-release || sudo dnf -y --nogpgcheck install http://download1.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm
rpm --quiet --query rpmfusion-nonfree-release || sudo dnf -y --nogpgcheck install http://download1.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm


sudo dnf -y install \
  aspell-de \
  aspell-en \
  autojump-zsh \
  cabal-install
  emacs \
  fedy \
  ctags-etags \
  iotop \
  docker-engine \
  lynx \
  nmap \
  pwgen \
  p7zip \
  ruby \
  ShellCheck \
  the_silver_searcher \
  tmux \
  unclutter \
  weechat \
  whois \
  xclip \
  zsh

# editorconfig

# if there is a graphical environment
if [[ -n "${DISPLAY}" ]]; then
  sudo dnf -y install \
    backintime-gnome \
    chromium \
    gnome-tweak-tool \
    gparted \
    inkscape \
    mixxx \
    vlc \
    zeal
fi
