name: qupl
root: ~/
windows:
  - editor:
      layout: even-horizontal
      panes:
        - pj qupl && e .
        - exit
        - pj qupl && ggpull
  - server:
      layout: even-horizontal
      panes: pj qupl && docker start qupl_postgres && bin/dev_server
